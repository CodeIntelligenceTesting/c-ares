.\"
.\" Copyright 2020 Danny Sonnenschein <my.card.god@web.de>
.\" SPDX-License-Identifier: MIT
.\"
.TH CI_PARSE_CAA_REPLY 3 "16 September 2020"
.SH NAME
ci_parse_caa_reply \- Parse a reply to a DNS query of type CAA
.SH SYNOPSIS
.nf
#include <ci.h>

int ci_parse_caa_reply(const unsigned char* \fIabuf\fP, int \fIalen\fP,
                         struct ci_caa_reply **\fIcaa_out\fP);
.fi
.SH DESCRIPTION
The
.BR "ci_parse_caa_reply" 
function parses the response to a query of type CAA into a
linked list (one element per sub-string) of
.IR "struct ci_caa_reply"
The parameters
.I abuf
and
.I alen
give the contents of the response.  The result is stored in allocated
memory and a pointer to it stored into the variable pointed to by
.IR caa_out .
It is the caller's responsibility to free the resulting
.IR caa_out
structure when it is no longer needed using the function
.B ci_free_data(3)
.PP
The structure 
.I ci_caa_reply(3)
contains the following fields:
.sp
.in +4n
.nf
struct ci_caa_reply {
  struct ci_caa_reply *next;
  int                    critical;
  unsigned char         *property;
  size_t                 plength; /* plength excludes null */
  unsigned char         *value;
  size_t                 length;  /* length excludes null */
};
.fi
.in
.PP
.SH RETURN VALUES
.BR "ci_parse_caa_reply"
can return any of the following values:
.TP 15
.B CI_SUCCESS
The response was successfully parsed.
.TP 15
.B CI_EBADRESP
The response was malformatted.
.TP 15
.B CI_ENODATA
The response did not contain an answer to the query.
.TP 15
.B CI_ENOMEM
Memory was exhausted.
.SH EXAMPLE
.nf
#include <arpa/inet.h>
#include <time.h>
#include <sys/time.h>
#include <netdb.h>

#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

#include "ci.h"

static void dns_callback(void *arg,
                         int status,
                         int timeouts,
                         unsigned char *abuf,
                         int alen)
  {
    struct ci_caa_reply *caa_out;
    int err;

    err = ci_parse_caa_reply (abuf, alen, &caa_out);
    if (err == CI_SUCCESS)
      {
        struct ci_caa_reply *caa_curr;
        for (caa_curr=caa_out; caa_curr; caa_curr=caa_curr->next)
          printf ("%s. CAA %i %s \\"%s\\"\\n", arg,
                                            caa_curr->critical,
                                            caa_curr->property,
                                            caa_curr->value);
      }
    else
      {
        printf ("err=%i\\n", err);
      }
    ci_free_data (caa_out);
  }

static void main_loop(ci_channel_t **channel)
  {
    int nfds, count;
    fd_set readers, writers;
    struct timeval tv, *tvp;
    while (1)
      {
        FD_ZERO (&readers);
        FD_ZERO (&writers);
        nfds = ci_fds (*channel, &readers, &writers);
        if (nfds == 0)
          break;
        tvp = ci_timeout (*channel, NULL, &tv);
        count = select (nfds, &readers, &writers, NULL, tvp);
        ci_process (*channel, &readers, &writers);
      }
  }

int main(int argc, char **argv)
  {
    const char *sversion;
    int iversion;
    int err;

    sversion = ci_version (&iversion);
    printf ("c-ci version %s\\n", sversion);

    char *domain = "wikipedia.org";
    if (argc > 1)
      domain = argv[1];

    ci_channel_t *channel;
    if ((err = ci_init (&channel)) != CI_SUCCESS)
      {
        printf ("ci_init() failed (%i)\\n", err);
        exit (EXIT_FAILURE);
      }

    ci_query (channel, domain,
                1,   /* ns_c_in */
                257, /* T_CAA */
                dns_callback, domain);

    main_loop (&channel);

    ci_destroy (channel);

    exit (EXIT_SUCCESS);
  }
.fi
.SH AVAILABILITY
This function was first introduced in c-ci version 1.17.0.
.SH SEE ALSO
.BR ci_query (3)
.BR ci_free_data (3)
