.\"
.\" Copyright 2010 by Ben Greear <greearb@candelatech.com>
.\" SPDX-License-Identifier: MIT
.\"
.TH CI_SET_SERVERS 3 "5 March 2010"
.SH NAME
ci_set_servers, ci_set_servers_ports \- Initialize name server configuration
for an ci channel. (deprecated)
.SH SYNOPSIS
.nf
#include <ci.h>

int ci_set_servers(ci_channel_t *\fIchannel\fP,
                     const struct ci_addr_node *\fIservers\fP)

int ci_set_servers_ports(ci_channel_t *\fIchannel\fP,
                           const struct ci_addr_port_node *\fIservers\fP)
.fi
.SH DESCRIPTION
The \fBci_set_servers(3)\fP function initializes name servers configuration
for the channel data identified by
.IR channel ,
from a
.IR servers
pointer to a linked list of ci_addr_node structs holding name servers
address data.
.PP
The name server linked list pointer argument may be the result of a previous
call to \fBci_get_servers(3)\fP or a linked list of \fBci_addr_node\fP structs
set up by other means.
.PP
The \fBci_set_servers_ports(3)\fP function also allows the specification of UDP and
TCP ports to be used for communication on a per-server basis.  The provided
linked list argument may be the result of a previous call to
\fBci_get_servers_ports(3)\fP or a linked list of \fBci_addr_port_node\fP structs
set up by other means.
.PP
This function replaces any potentially previously configured name servers
with the ones given in the linked list. So, in order to configure a channel
with more than one name server all the desired ones must be specified in a
single list. Though not recommended, passing NULL will clear all configured
servers and make an inoperable channel, this may be advantageous for test
simulation but unlikely to be useful in production.
.PP
The function does not take ownership of the linked list argument.
The caller is responsible for freeing the linked list when no longer needed.
.PP
This function is capable of handling IPv4 and IPv6 name server
addresses simultaneously, rendering \fBci_init_options(3)\fP with
optmask \fBCI_OPT_SERVERS\fP functionally obsolete except for
IPv4-only name server usage.
.PP
As of v1.22.0 this function can
be called on an active channel with running queries, previously it would return
CI_ENOTIMP.

.SH RETURN VALUES
.B ci_set_servers(3)
may return any of the following values:
.TP 15
.B CI_SUCCESS
The name servers configuration was successfully initialized.
.TP 15
.B CI_ENOMEM
The process's available memory was exhausted.
.TP 15
.B CI_ENODATA
The channel data identified by 
.IR channel
was invalid.
.TP 15
.B CI_ENOTINITIALIZED
c-ci library initialization not yet performed.
.SH SEE ALSO
.BR ci_set_servers_csv (3),
.BR ci_get_servers (3),
.BR ci_init_options (3),
.BR ci_dup (3)

.SH NOTES
Deprecated functions as of c-ci 1.24.0 due to inability to set all available
server options.  Use \fBci_set_servers_csv(3)\fP.

.SH AVAILABILITY
\fBci_set_servers(3)\fP was added in c-ci 1.7.1;
\fBci_set_servers_ports(3)\fP was added in c-ci 1.11.0.
